#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

void permutations(int n, int len, vector<int> sol)
{
    
}

int main()
{
    int N, i, j, k;
    cin >> N >> i >> j >> k;
    permutations()
}